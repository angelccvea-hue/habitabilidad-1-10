# Copiar a secrets.toml en local (NO subir a Git).
# En servidor: preferir variables de entorno (BI_PASSWORD, BI_DATA_KEY, etc.)
# o Secrets de Streamlit.

[auth]
password = "cambiar-esta-clave-institucional"

# Producción (alternativa a BI_ENV=production):
# BI_REQUIRE_AUTH=1
# BI_PASSWORD=...
